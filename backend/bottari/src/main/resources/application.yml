spring:
  application:
    name: bottari
  jpa:
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
    open-in-view: true
  sql:
    init:
      mode: always

firebase:
  service:
     key-path: ${FIREBASE_SERVICE_ACCOUNT_JSON}
