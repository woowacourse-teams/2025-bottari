package com.bottari.presentation.view.edit.alarm.listener

import com.bottari.presentation.model.DayOfWeekUiModel

fun interface OnDayOfWeekClickListener {
    fun onClick(dayOfWeek: DayOfWeekUiModel)
}
